load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "time",
    srcs = glob(["*.cc"]),
    hdrs = glob(["*.h"]),
    strip_include_prefix = "//src",
    visibility = ["//visibility:public"],
    deps = [
        "//src/runtime",
        "@spdlog",
    ],
)
