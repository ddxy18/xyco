flag_management:
  default_rules:
    statuses:
      - type: project
        target: 90%
      - type: patch
        target: 90%
  individual_flags:
    - name: epoll
      paths:
        - "src/"
        - "!src/fs/io_uring/"
        - "!src/io/io_uring/"
        - "!src/net/io_uring/"
    - name: uring
      paths:
        - "src/"
        - "!src/fs/epoll/"
        - "!src/io/epoll/"
        - "!src/net/epoll/"
    - name: tests
      paths:
        - "tests/"
      statuses:
        - type: project
          target: 98%
